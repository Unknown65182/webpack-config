import { Configuration, DefinePlugin } from "webpack";

const config: Configuration = {
  mode: "production",
  target: "browserslist",
  devtool: "source-map",
  plugins: [],
};

export default config;
